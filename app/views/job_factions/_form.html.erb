<%= form_with(model: [@campaign, @job, job_faction], local: true) do |form| %>
  <% if job_faction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_faction.errors.count, "error") %> prohibited this job_faction from being saved:</h2>

      <ul>
      <% job_faction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :faction_id %>
    <%= form.collection_select :faction_id,
    Faction.order(:name), :id, :name %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
