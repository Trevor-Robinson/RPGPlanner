<%= form_with(model: [@campaign, @faction, turf], local: true) do |form| %>
  <% if turf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turf.errors.count, "error") %> prohibited this turf from being saved:</h2>

      <ul>
      <% turf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :system_id %>
    <%= form.collection_select :system_id, System.order(:name), :id, :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
